# JPS Admin Panel - Root .htaccess
# Deny access to all files by default
# Only the public/ directory should be accessible via web

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
